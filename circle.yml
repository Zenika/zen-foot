general:
  branches:
    only:
      - dev # devs
#      - release
machine:
  java:
    version: oraclejdk7
  environment:
    APP_ENGINE_SDK_VERSION: 1.9.27
#dependencies:
#  pre:
#    - curl -o $HOME/appengine-java-sdk-$APP_ENGINE_SDK_VERSION.zip https://storage.googleapis.com/appengine-sdks/featured/appengine-java-sdk-$APP_ENGINE_SDK_VERSION.zip
#    - unzip -q -d $HOME $HOME/appengine-java-sdk-$APP_ENGINE_SDK_VERSION.zip
test:
  override:
    - mvn test
#deployment:
#  appengine:
#    branch: dev
#    commands:
#       - mvn -DskipTests package
#       - echo $APPENGINE_PASSWORD | $HOME/appengine-java-sdk-$APP_ENGINE_SDK_VERSION/bin/appcfg.sh --email=$APPENGINE_EMAIL --passin update target/zen-foot-1
