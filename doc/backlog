
=== URGENT

=== NOW
- Formulaire d'envoi de mail pour l'admin -> chaque player
- Remettre en service l'inscription par email (desactiver l'inscription automagique !)
- Mettre la date au début de incoming matches
- Ajouter un champ String "comments" sur un Match ou on peut remplir par exemple (TAB 2:3)
- ajouter la possibilité de modifier les résultats d'un match passe pour la partie admin
- une icone de point d'interrogation qui fasse surgir une bulle d'aide, avec ton texte de la FAQ . . .

=== LATER
- Ajouter un bouton "Valider/Rafraichir mes paris" avec une icône sympa
- change password
- refactoring wicket
- Sectionner la liste des matchs
- Service de mails de rappels avant les matchs (3 jours ?)
- Service de mail pour les admin pour leur rappeler de mettre à jour les résultas de matchs

=== MAYBE
- faire la verification qu'une meme equipe ne peut pas jouer deux matchs a la meme date
- 2 classements (pts - pts pondere par nb paris) !?
- FORUM intégré
- Fil de news (ex: Fifa depuis la page d'accueil)
- Mettre plus en avant les matchs à venir car c'est cela l'interessant
- Design
-- Animations... ballon animé (tourne sur lui même, ...)
-- Look trop sobre (Ex: tête de zidane en gros plans ... !?)
-- différencier les titres grisé

=== DONE
- Vérifier les CASCADE hibernate en particulier lors de la suppression d'un user ou d'une team
-- DELETE Bet -> rien
-- DELETE Player -> DELETE Bet
-- DELETE Match -> DELETE Bet
-- DELETE Team -> DELETE Match -> DELETE Bet
- coloration selont le nombre de points
- Ajouter une page de feedback [Une question ? Une remarque !]
- FAQ under Feedback -- Comment valider mes paris ?
- afficher la version dans le footer en le lisant depuis le properties ! ainsi que le titre et sous titre...
- Ajouter le CRUD sur les teams
- Précisez dans les règles si les buts sont 1. fin de matchs, 2. prolongation, 3. penalty => On décide que 2. Prolongation
- Vérifier que si un player laisse ouvert son navigateur alors qu'un match commence, il ne peut pas mettre à jour ses scores
- Enlever le CEST "z"
- ajouter nos paris dans liste matchs en cours
- ajouter nos paris dans liste matchs passe
- production mode
- Auto inscription
- Lost pwd % admin
- AOP service généralise Spring doc 2.5.x : 6.2.3.4
- Save return void !
- Restrictions.naturalid
- WIA DAO p343
- Natural id hbm
- Cascade HBM
- Hbm notnull=true unique=true
- Equals & hashcode
- sécuriser la page admin
- ajouter Spring
- ajouter hibernate
- ajouter scripts sql
- préparer environnement de PROD
OHU - ajouter service de mail
OHU - ajouter fichier properties
MCA - ajouter Spring
MCA - ajouter hibernate
MCA - ajouter scripts sql
OHU - préparer environnement de PROD
